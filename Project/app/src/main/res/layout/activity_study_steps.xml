<?xml version="1.0" encoding="utf-8"?>
<!-- Layout für die StudyStepsActivity – angepasst an das Dashboard-Farbschema -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f9f9f9">

    <!-- Titel-Eingabe für Study Step -->
    <EditText
        android:id="@+id/etStepTitle"
        android:hint="Titel des Schritts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:textSize="16sp"
        android:backgroundTint="#00897B" />

    <!-- Datum-Eingabe für Study Step -->
    <EditText
        android:id="@+id/etStepDate"
        android:hint="Fälligkeitsdatum (YYYY-MM-DD)"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:layout_marginTop="12dp"
        android:textSize="16sp"
        android:backgroundTint="#00897B" />

    <!-- Button zum Hinzufügen -->
    <Button
        android:id="@+id/btnAddStep"
        android:text="Schritt hinzufügen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:backgroundTint="#00796B"
        android:textColor="#FFFFFF" />

    <!-- Platzhalter-Text, wenn noch keine Schritte da sind -->
    <TextView
        android:id="@+id/tvEmptySteps"
        android:text="Keine Schritte vorhanden"
        android:visibility="gone"
        android:gravity="center"
        android:textColor="#888888"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp" />

    <!-- RecyclerView für bestehende Study-Steps -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerStudySteps"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:clipToPadding="false" />

    <!-- Zurück-Button -->
    <Button
        android:id="@+id/btnBackToDashboard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Zurück"
        android:layout_marginTop="12dp"
        android:backgroundTint="#B0BEC5"
        android:textColor="#000000" />
</LinearLayout>
